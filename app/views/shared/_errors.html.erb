<% if record.errors.any? %>
  <div class="errors">
    <strong><%= record.errors.count %> kesalahan perlu diperbaiki:</strong>
    <ul>
      <% record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
